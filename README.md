Install the "nodemon" module the TypeScript compiler globally:

> npm typescript nodemon -g

Install packages from the package.json file:

> npm i

Build:

> npm run build

Run the server locally:

> nodemon dist/App.js

Go to the browser and type:

> localhost:3000